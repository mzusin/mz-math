export declare const mod: (n: number, m: number) => number;
/**
 * Convert range [a, b] to [c, d].
 * f(x) = (d - c) * (x - a) / (b - a) + c
 */
export declare const convertRange: (a: number, b: number, c: number, d: number, x: number) => number;
